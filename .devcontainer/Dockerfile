FROM golang:1.24

# Install any extra tools you want
RUN apt-get update && apt-get install -y git curl unzip

# Optional: set up your workdir
WORKDIR /workspace

# Optional: copy your source code
# COPY . .

# Optional: set environment variables
ENV GO111MODULE=on
